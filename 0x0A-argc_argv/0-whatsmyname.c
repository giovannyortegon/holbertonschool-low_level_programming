#include <stdio.h>

/**
 * main - entry point
 * @argc: number of arguments
 * @argv: double pointer
 * Return: Always 0 (Success)
 */
int main(int argc, char **argv)
{
	printf("%s\n", argv[0]);
	return (0);
}
